<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Jewels" parent="@android:style/Theme.DeviceDefault" />

    <style name="Theme.Jewels.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Jewels</item>
    </style>
</resources>